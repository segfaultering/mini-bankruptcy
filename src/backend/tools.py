from langchain.tools import tool


# Manager Agent
@tool(content_and_artifact
def create_tasks(tasks: str) -> :
    """Creates a checklist file that the manager agent stores tasks for the team to work towards."""
            















